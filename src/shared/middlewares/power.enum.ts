export enum Environment {
  LIVE = 'Live',
  SANDBOX = 'Sandbox',
}

export enum RequestType {
  VERIFICATION = 'verification',
  VEND = 'vend',
  REQUERY = 'requery',
  GENERAL = 'general',
}

export enum Service {
  Power = 'power',
}
